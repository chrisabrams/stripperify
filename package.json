{
  "name": "stripperify",
  "version": "0.0.0",
  "description": "Browserify transformation that strips out an Array of strings from a stream.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Abrams",
  "license": "MIT",
  "dependencies": {
    "through": "~2.3.4"
  }
}
